//another way to create functions , shorter way
//use them as callbacks
//we dont need to write function to create function
let sum = (a,b) => { //no function keyword
    return a+b;
}
let hello = () => { //whille returning or using code use {} brackets otherwise ()
    console.log("hello world");
}

//implicit return = when we only need to return the value nothing else
let sub = (a,b) => (
    a-b //no need to write return to return
    //remember no   and no {} only use ()
);





